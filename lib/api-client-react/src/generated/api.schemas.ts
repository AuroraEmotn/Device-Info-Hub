/**
 * Generated by orval v8.18.0 🍺
 * Do not edit manually.
 * Api
 * Device information lookup API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface DeviceLookupInput {
  /** Device name or model number (e.g. "Samsung Galaxy S21", "iPhone 13 Pro", "Redmi Note 10") */
  query: string;
  /** Language code for the response (e.g. "ar", "en", "fr", "tr"). Defaults to "ar". */
  language?: string;
}

export interface CustomRom {
  /** ROM name (e.g. LineageOS, PixelExperience) */
  name: string;
  /** Android version this ROM is based on */
  androidVersion: string;
  /** Status (e.g. Official, Unofficial, Discontinued) */
  status: string;
  /**
     * ROM maintainer name
     * @nullable
     */
  maintainer?: string | null;
}

export interface CustomRecovery {
  /** Recovery name (e.g. TWRP, OrangeFox, SHRP) */
  name: string;
  /** Whether it is an official build or a port */
  isOfficial: boolean;
  /**
     * Additional notes about this recovery
     * @nullable
     */
  notes?: string | null;
}

export interface DeviceInfo {
  /** Whether a matching device was found */
  found: boolean;
  /** Full device name */
  name: string;
  /**
     * Device brand/manufacturer
     * @nullable
     */
  brand?: string | null;
  /** Official release date */
  releaseDate: string;
  /** Operating system with UI skin (e.g. Android 11 + One UI 3.1) */
  os: string;
  /** Storage options (e.g. 128GB / 256GB) */
  storage: string;
  /** RAM options (e.g. 6GB / 8GB) */
  ram: string;
  /** Processor performance rating from 1 to 10 */
  processorRating: number;
  /**
     * Processor/chipset name
     * @nullable
     */
  processorName?: string | null;
  /** Latest official OS version from manufacturer */
  latestOfficialOs: string;
  /** Available custom ROMs */
  customRoms: CustomRom[];
  /** Available custom recovery systems */
  customRecoveries: CustomRecovery[];
  /** Latest Android version achievable via custom ROMs */
  latestAndroidViaRom: string;
  /**
     * Additional notes about the device
     * @nullable
     */
  notes?: string | null;
}

export interface DeviceSuggestion {
  name: string;
  brand: string;
}

export interface UsedDeviceEvaluationInput {
  /** Full device name as returned from lookup */
  deviceName: string;
  /** Language code (ar, en, fr, tr) */
  language?: string;
  /** Overall condition (excellent / very_good / good / acceptable / poor) */
  condition: string;
  /** Storage variant of the unit (e.g. 128GB) */
  storage: string;
  /** RAM variant of the unit (e.g. 6GB) */
  ram: string;
  /** Screen condition (perfect / scratches / cracked / replaced) */
  screenCondition?: string;
  /**
     * Battery health percentage if known (e.g. 85)
     * @nullable
     */
  batteryHealth?: number | null;
  /** Whether the device is carrier/SIM locked */
  isLocked?: boolean;
  /**
     * Any known defects or issues described by the user
     * @nullable
     */
  defects?: string | null;
  /**
     * Price the seller is asking (in local currency)
     * @nullable
     */
  askingPrice?: number | null;
  /**
     * Currency code (e.g. USD, DZD, SAR, EUR)
     * @nullable
     */
  currency?: string | null;
}

export interface UsedDeviceEvaluation {
  /** Lower bound of fair market price */
  fairPriceMin: number;
  /** Upper bound of fair market price */
  fairPriceMax: number;
  /** Currency used for the price estimate */
  currency: string;
  /** excellent / good / fair / overpriced */
  dealRating: string;
  /** Localized deal rating label */
  dealRatingLabel: string;
  /** Overall condition score 1-10 */
  score: number;
  /** Short summary verdict about this deal */
  verdict: string;
  /** Positive aspects of this unit */
  strengths: string[];
  /** Negative aspects or risks */
  weaknesses: string[];
  /** Tips for negotiating a better price */
  negotiationTips: string[];
  /** Things to verify before finalizing the purchase */
  checklistBeforeBuying: string[];
}

export interface ErrorResponse {
  error: string;
}

